#!/usr/bin/bash

set -x

rm -rf ../SHiELD_SRC/*
rm -rf ../SERIALBOX_BUILD/*
./CHECKOUT_code

cd Build

##### SHiELD DEBUG - noserial sanity check
#rm build_shield_nh.debug.64bit.gnu.out
#./COMPILE cleanall shield nh debug 64bit gnu noserial

##### SHiELD DEBUG - actually useful one
#rm build_shield_nh.debug.64bit.gnu.out
./COMPILE cleanall shield nh debug 64bit gnu serial

cd ..
set +x
